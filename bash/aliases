alias rakenuke="rake db:drop; rake db:create; rake db:migrate; rake db:seed; rake db:test:prepare"
alias rakenuke_alt="rake db:drop; rake db:create; rake db:schema:load; rake db:test:prepare"

alias ssdb="psql -Upostgres ss_development"
alias new_seed="rails g seed_file"

alias start_postgres="postgres -D /usr/local/var/postgres"
alias start_postgres_alt="pg_ctl -D /usr/local/var/postgres -l logfile start"
#edit /ect/resolv.conf  to use 127.0.0.1 as first nameserver - or go to network panel/advanced / dns and add 127.0.0.1
alias start_bind="sudo named -f -c /etc/named.conf"


##Create a db in postgres with username studentstock
#createdb -O studentstock ss_development
#createdb -O studentstock ss_test
#createdb -O studentstock ss_production
alias sbump="ssh -i ~/mcarrmicro.pem ec2-user@bump"
alias sdream=sbump
